#include <iostream>
using namespace std;

int main(){
  
  // operator pada bitwise
  
  // &  = bitwise AND
  // |  = bitwise OR 
  // ^  = bitwise XOR
  // ~  = bitwise NOT
  // << = bitwise SHL (Shift Bits Left)
  // >> = bitwise SHR (shift Bits Right)
  
  return 0;
}